= simple_form_for([@current_phone, @reminder], html: { class: 'form-horizontal' }) do |f|
  = f.input :title
  = f.input :body
  = f.input :scheduled_for, as: :datetime
  = f.submit 'Submit', class: 'btn btn-primary'
